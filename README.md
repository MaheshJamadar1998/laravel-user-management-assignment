Laravel User Management Assignment

Architecture:
Controller → Service → BO → DAO → Database

Features:
• Create User
• Update User
• Retrieve User
• FormRequest Validation
• Password Hashing
• Laravel Caching

API Endpoints:

POST /api/users
PUT /api/users/{id}
GET /api/users/{id}

Technologies:
Laravel 10
MySQL
Laravel Cache
